# ── Dashboard runtime (shiny_app/) ────────────────────────────────────────────
shiny>=1.6.0
shinywidgets>=0.8.0
plotly>=5.20.0
pandas>=2.0.0
pyarrow>=14.0.0
numpy>=1.24.0
matplotlib>=3.8.0
wordcloud>=1.9.3
python-dotenv>=1.0.0
requests>=2.31.0          # DeepSeek chatbot HTTP calls

# ── Forecast models (forecasting/forecast_models.py) ──────────────────────────
# ARIMA and Holt-Winters run in this venv:
statsmodels>=0.14.0
scikit-learn>=1.3.0
# Prophet requires a separate venv — see requirements-model.txt
# TimesFM requires a separate cloned repo + venv — see docs/timesfm_setup.md

# ── Data pipeline (scripts/data_collection.py) ────────────────────────────────
# Only needed to regenerate GDELT data from BigQuery:
google-cloud-bigquery>=3.17.0
db-dtypes>=1.2.0
beautifulsoup4>=4.12.0
